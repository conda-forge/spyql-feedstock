{% set name = "spyql" %}
{% set version = "0.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/spyql-{{ version }}.tar.gz
  sha256: 768b722ec31105ffbfef275f059159fdb1df79767dda2bb21d4868ee406106ca

build:
  entry_points:
    - spyql=spyql.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - asciichartpy
    - click >=7.1.2
    - python >=3.6
    - tabulate

test:
  source_files:
    - tests
  imports:
    - spyql
  commands:
    - pip check
    - spyql --help
    - pytest -vvv --capture=tee-sys tests
  requires:
    - numpy
    - orjson
    - pip
    - pytest

about:
  home: https://github.com/dcmoura/spyql
  summary: A query language that combines the structure of SQL with the power Python
  doc_url: https://spyql.readthedocs.io/en/latest/
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
